# Prevent psplash-systemd from automatically quitting psplash when
# graphical.target is reached.  The kiosk launcher script will send
# QUIT to psplash manually, right before Chromium takes over the screen,
# guaranteeing a seamless transition with no login prompt visible.
[Unit]
ConditionPathExists=/nonexistent-psplash-disabled-by-kiosk

