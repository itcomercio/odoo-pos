# Keep psplash-systemd active for progress, but replace its helper so it does
# not auto-send QUIT at 100%. The kiosk launcher will dismiss psplash manually.
[Service]
ExecStart=
ExecStart=-/usr/bin/psplash-progress-helper.sh
