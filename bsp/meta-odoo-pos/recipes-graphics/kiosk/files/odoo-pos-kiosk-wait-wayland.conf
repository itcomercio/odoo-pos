[Unit]
After=weston.service
Requires=weston.service

[Service]
ExecStartPre=/bin/sh -c 'for i in $(seq 1 60); do if [ -S "${XDG_RUNTIME_DIR:-/run}/wayland-0" ] || [ -S /run/wayland-0 ]; then exit 0; fi; sleep 1; done; echo "wayland socket not ready" >&2; exit 1'

