# Disable getty login prompt on VT1 — the kiosk system must never show
# a login screen.  Serial console login (getty@ttyS0) is left intact
# for maintenance access.
[Unit]
ConditionPathExists=/nonexistent-getty-disabled-by-kiosk

